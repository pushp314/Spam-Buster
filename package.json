{
  "name": "spam-buster-root",
  "version": "1.0.0",
  "dependencies": {
    "concurrently": "^8.2.0"
  },
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "dev:all": "concurrently \"cd server && npm run dev:all\" \"cd client && npm run dev\"",
    "redis:start": "brew services restart redis"
  }
}
